namespace Common
{
    public enum ParamererCode:byte//区分传送数据的时候，参数的类型
    {
        Username,
        Password,
        Position,
        X,Y,Z,
        UsernameList,
        PlayerDataList,
    }
}
